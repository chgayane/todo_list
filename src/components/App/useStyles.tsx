import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  wrapper: {
    margin: "auto",
    width: 580,
    minHeight: 500,
    backgroundColor: "#58606e",
    padding: "10px",
  },
});
