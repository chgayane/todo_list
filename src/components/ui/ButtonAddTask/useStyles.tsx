import { makeStyles } from "@mui/styles";


export const useStyles = makeStyles({
  btn: {
    cursor: "pointer",
    backgroundColor: "transparent",
    border: "none",
    color: "#d1acb9",
    marginLeft: "40px",
  },
});

