import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  wrapper: {
    display: "flex",
    justifyContent: "space-between",
    margin: "10px 0px",
    borderRadius: 1,
  },
});
